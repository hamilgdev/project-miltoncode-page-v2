import React from "react";
import PropTypes from "prop-types";

const Header = () => {
  return <header className="bg-c-first-main">header</header>;
};

Header.propTypes = {};

export default Header;
